#pragma once

#include <wd/common>

namespace wd {
	namespace packet {
		typedef struct pcap_pkthdr header;

		void pack(msgpack::packer<std::ostream>& packer, int32_t pid, const header* header, const uint8_t* packet);
	}
}
